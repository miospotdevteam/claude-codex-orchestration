---
name: codex-dispatch
description: Dispatch Codex and Grok implementation or verification work through direction-locked wrappers from a Codex-native conductor, then persist only parsed contract results. Use for every runnable implementation step and every independent verifier pass.
---

# Codex host entrypoint

This conventional Codex plugin entrypoint delegates to the canonical host body
at `../../../codex-skills/codex-dispatch/SKILL.md`, relative to this `SKILL.md`.
Read that file completely before dispatching any lane and follow it as this
skill's authoritative instructions. If it is missing or unreadable, stop
fail-closed; never invoke a provider CLI directly.
