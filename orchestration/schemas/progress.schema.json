{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://orchestration.claude/schemas/progress.schema.json",
  "title": "Progress",
  "description": "Mutable v2 orchestration plan execution state.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "planId",
    "startedAt",
    "lastUpdatedAt",
    "currentFrontier",
    "steps"
  ],
  "properties": {
    "planId": {
      "type": "string"
    },
    "startedAt": {
      "type": "string",
      "format": "date-time"
    },
    "lastUpdatedAt": {
      "type": "string",
      "format": "date-time"
    },
    "currentFrontier": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "steps": {
      "type": "object",
      "additionalProperties": {
        "$ref": "#/$defs/stepProgress"
      }
    },
    "deviations": {
      "type": "array",
      "description": "Plan-level deviations: events that changed how the plan as a whole executes (a panel lane failing, an executor rerouted for quota, a model id migration). Step-scoped divergences belong in steps.<id>.deviations instead.",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "at",
          "note"
        ],
        "properties": {
          "at": {
            "type": "string",
            "format": "date-time"
          },
          "note": {
            "type": "string",
            "minLength": 1
          }
        }
      }
    }
  },
  "$defs": {
    "path": {
      "type": "string",
      "minLength": 1
    },
    "dispatch": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "executor",
        "model",
        "startedAt"
      ],
      "properties": {
        "executor": {
          "enum": [
            "codex",
            "grok",
            "claude"
          ]
        },
        "model": {
          "type": "string"
        },
        "startedAt": {
          "type": "string",
          "format": "date-time"
        }
      }
    },
    "stepProgress": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "status"
      ],
      "properties": {
        "status": {
          "enum": [
            "pending",
            "in_progress",
            "done",
            "blocked",
            "skipped"
          ]
        },
        "startedAt": {
          "type": "string",
          "format": "date-time"
        },
        "completedAt": {
          "type": "string",
          "format": "date-time"
        },
        "owner": {
          "enum": [
            "codex-impl",
            "claude-impl",
            "grok-impl",
            "manual"
          ]
        },
        "dispatch": {
          "$ref": "#/$defs/dispatch"
        },
        "verdict": {
          "description": "Authoritative single-verdict field (legacy flat record). Lane-scoped history lives under verdicts.",
          "enum": [
            "PASS",
            "FINDINGS",
            "FAIL"
          ]
        },
        "findings": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "result": {
          "type": "string"
        },
        "verdicts": {
          "description": "Per-lane verifier records (codex / grok). Optional; dual-mandate done-gating reads these.",
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "codex": {
              "$ref": "#/$defs/laneVerdict"
            },
            "grok": {
              "$ref": "#/$defs/laneVerdict"
            }
          }
        },
        "deviations": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/deviation"
          }
        },
        "filesTouched": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/path"
          }
        }
      }
    },
    "laneVerdict": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "verdict",
        "summary",
        "findings",
        "filesTouched",
        "timestamp"
      ],
      "properties": {
        "verdict": {
          "enum": [
            "PASS",
            "FINDINGS",
            "FAIL"
          ]
        },
        "summary": {
          "type": "string"
        },
        "findings": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "filesTouched": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/path"
          }
        },
        "timestamp": {
          "type": "string",
          "format": "date-time"
        }
      }
    },
    "deviation": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "type",
        "description",
        "files"
      ],
      "properties": {
        "type": {
          "type": "string"
        },
        "description": {
          "type": "string"
        },
        "files": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/path"
          }
        }
      }
    }
  }
}